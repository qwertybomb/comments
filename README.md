# comments
a program that displays the comments found in a source file

# building
to build the program run build.bat

# Usage
use `comments --help` to find out how to use the program

for example `comments -l -hcc comments.c` will display each c/c++ comment in comments.c with the line number and without showing the comment count
